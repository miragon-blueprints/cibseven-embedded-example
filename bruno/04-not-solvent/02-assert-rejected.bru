meta {
  name: Assert the DMN rejected the application
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/bike-leasing/{{applicationId}}
  body: none
  auth: none
}

script:pre-request {
  await pollApp("/api/bike-leasing/" + bru.getVar("applicationId"),
    (b, s) => s === 200 && b && b.status === "REJECTED");
}

assert {
  res.status: eq 200
  res.body.status: eq REJECTED
}
